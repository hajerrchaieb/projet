name: ESP32 Matter CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install system dependencies
      run: |
        sudo apt update
        sudo apt install -y git wget curl build-essential cmake ninja-build \
        python3 python3-pip python3-venv

    # Clone ESP-Matter ONLY
    - name: Clone ESP-Matter
      run: |
        git clone --recursive https://github.com/espressif/esp-matter.git
        cd esp-matter
        git submodule update --init --recursive
        echo "ESP_MATTER_PATH=$PWD" >> $GITHUB_ENV

    # Install IDF from ESP-Matter
    - name: Install ESP-IDF (from esp-matter)
      run: |
        cd $ESP_MATTER_PATH
        ./install.sh
        echo "IDF_PATH=$ESP_MATTER_PATH/esp-idf" >> $GITHUB_ENV

    - name: Build Light Example
      run: |
        source $ESP_MATTER_PATH/export.sh
        cd $ESP_MATTER_PATH/examples/light
        idf.py set-target esp32c3
        idf.py build

    - name: Upload firmware
      uses: actions/upload-artifact@v4
      with:
        name: firmware
        path: esp-matter/examples/light/build/*.bin

