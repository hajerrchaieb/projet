
name: ESP32 Matter CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Update system and install dependencies
      run: |
        sudo apt update && sudo apt upgrade -y
        sudo apt install -y git wget curl build-essential cmake ninja-build \
        python3 python3-pip python3-venv unzip libffi-dev libssl-dev

    - name: Clone and setup ESP-IDF
      run: |
        cd ~
        git clone https://github.com/espressif/esp-idf.git
        cd esp-idf
        git checkout v5.4.1
        
        git clean -ffd
        git submodule update --init --depth 1
        
        find . -type d -name '.git' ! -path './.git/*' | while read gitdir; do
          gitparent=$(dirname "$gitdir")
          cd "$gitparent" && git clean -ffd && cd - > /dev/null
        done
        
        git submodule foreach --recursive git clean -ffd
        git submodule update --init --depth 1 --recursive
        
        ./install.sh
        export IDF_CCACHE_ENABLE=1
        echo "IDF_PATH=$HOME/esp-idf" >> $GITHUB_ENV
        echo "IDF_CCACHE_ENABLE=1" >> $GITHUB_ENV

    - name: Clone and setup ESP-Matter
      run: |
        cd ~
        git clone https://github.com/espressif/esp-matter.git
        cd esp-matter
        
        git clean -ffd
        git submodule update --init --depth 1
        
        find . -type d -name '.git' ! -path './.git/*' | while read gitdir; do
          gitparent=$(dirname "$gitdir")
          cd "$gitparent" && git clean -ffd && cd - > /dev/null
        done
        
        git submodule foreach --recursive git clean -ffd
        git submodule update --init --depth 1 --recursive
        
        # DON'T run install.sh - it tries to build for Linux host
        # cd connectedhomeip/connectedhomeip
        # ./scripts/checkout_submodules.py --platform esp32 linux --shallow
        # cd ../..
        # ./install.sh
        
        echo "ESP_MATTER_PATH=$HOME/esp-matter" >> $GITHUB_ENV

    - name: Build firmware for ESP32C3
      run: |
        source $HOME/esp-idf/export.sh
        cd $HOME/esp-matter/examples/light
        idf.py set-target esp32c3
        idf.py build

    - name: Upload firmware artifact
      uses: actions/upload-artifact@v4
      with:
        name: light-firmware
        path: ~/esp-matter/examples/light/build/*.bin


